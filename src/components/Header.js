import React from 'react'
import { Navbar } from 'react-bootstrap';

export const Header = props => (
  <Navbar bg="primary" variant="dark">
    <Navbar.Brand href="#home">Randon Phone Number Generator</Navbar.Brand>
  </Navbar>
)